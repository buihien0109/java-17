import java.util.Random;

public class WhilePractice {
    public static void main(String[] args) {
        Random rd = new Random();
        int rdNumber = rd.nextInt(101); // 90

        while (true) {
            // Lấy thông tin nhập vào từ bàn phím

            // Kiểm tra thông tin
            // Nếu !- 0 => break để kết thúc vòng lặp
            // Nếu = 0 => cho nhập lại
        }
    }
}
